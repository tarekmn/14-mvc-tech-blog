{{#each texts as |text| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{text.title}}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{{text.description}}}</h3>
  </div>
</div>
{{/each}}
